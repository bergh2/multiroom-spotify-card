{
  "name": "spotify-media-card",
  "version": "0.1.0",
  "description": "Home Assistant card: start Spotify playlists on multi-room Chromecast speakers via Music Assistant",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "test": "vitest run",
    "deploy": "npm run build && node scripts/deploy.mjs"
  },
  "license": "MIT",
  "devDependencies": {
    "lit": "^3.3.3",
    "typescript": "^5.9.3",
    "vite": "^6.4.3",
    "vitest": "^3.2.7"
  }
}
